{
  "rules": {
    "spotifyAccessToken": {
      "$uid": {
        ".read": "auth.uid === $uid",
        ".write": "auth.uid === $uid"
      }
    },
    "parties": {
      ".read": "true",
      ".write": "true"
    }
  }
}
